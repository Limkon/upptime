# ===============================
# Do not edit this file directly!
# ===============================
#
# Your changes will be overwritten when the Upptime template updates (by default, weekly)
# Instead, change .upptimerc.yml configuration and the workflows will be generated accordingly.
#
# 🔼 Upptime @v1.39.0
# GitHub-powered open-source uptime monitor and status page by Anand Chowdhary
#
# * Source: https://github.com/upptime/upptime
# * Docs and more: https://upptime.js.org
# * More by Anand Chowdhary: https://anandchowdhary.com

name: Updates CI

on:
# schedule:
#   - cron: "0 3 * * *"  # Runs daily at 3:00 AM UTC
# repository_dispatch:
#   types: [updates]     # Triggered by external "updates" event
  workflow_dispatch:     # Allows manual triggering

jobs:
  release:
    name: Deploy updates
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}          # Checkout the triggering branch
          token: ${{ secrets.GH_PAT || github.token }}  # Use GH_PAT or default token

      - name: Update code
        uses: upptime/updates@master         # Syncs updates from Upptime
        env:
          GH_PAT: ${{ secrets.GH_PAT || github.token }}  # Pass token to action
